apiVersion: batch/v1
kind: Job
metadata:
  name: db-fixture
spec:
  template:
    spec:
      containers:
        - name: db-fixture
          image: 22i0765/db-fixture:latest
      restartPolicy: OnFailure